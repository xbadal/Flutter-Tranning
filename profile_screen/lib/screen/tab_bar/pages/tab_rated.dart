import 'package:flutter/material.dart';

class TabRated extends StatelessWidget {
  const TabRated({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.amber,);
  }
}
